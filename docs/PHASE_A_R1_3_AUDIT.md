# Phase A R1.3 — Manual-smoke audit

> R1.3 shipped 39/39 automated assertions green. Owner manual smoke found the same 6 bugs R1.3 was supposed to fix. This document reconciles the gap, screenshot by screenshot.
>
> Skeleton committed before any code. Columns 4 (screenshot), 5 (gap cause), and 6 (fix) are filled in only when the owner's screenshots arrive. Until then this file is the contract for what must happen next.

## Rule (R1.3-D13)

If owner manual smoke fails, the sub-R fails — regardless of automated pass rate. R1.3 "passed" 39 assertions and still failed on a real device. The automated number means nothing in isolation.

## Audit table

| # | Bug | R1.3 assertion | What the assertion verified (and passed) | What the screenshot shows | Why the assertion missed it | Fix |
|---|---|---|---|---|---|---|
| 1 | **B1** — Navbar not sticky on mobile (scrolls up with content) | `phase-a-r1-3-fixes.spec.ts:42` "nav has position: sticky and html[data-scrolled] toggles past 4px" | Computed `position` on `nav.nav` === `sticky` at viewport 375. `html[data-scrolled]` attribute toggled after scrollY > 4. | Screenshots 1–5 don't capture scroll behaviour. Cannot confirm/deny from this set. Need a scroll-down screenshot to verify. | Assertion verified the property is set but never simulated real scroll past the iOS URL-bar collapse. Possible the navbar is sticky to `<body>` but body height changes when iOS URL bar collapses, causing apparent scroll. | Pending more evidence. Treat as UNVERIFIED, not PASS, not FAIL. |
| 2 | **B2** — Login layout broken / role chips wrong on mobile | `phase-a-r1-3-fixes.spec.ts:145` "login role tabs render as 2-column grid at 375 (assert via child positions)" + `:166` form-side max-width 420 + `:176` no horizontal overflow | 5 chips at 375 produced 3 unique offsetTop values (= 3 rows of 2+2+1). Form-side computed max-width === "420px". `scrollWidth - innerWidth ≤ 2`. | Screenshot 5: role chips ARE 2-col (دانشجو / استاد top row, مدیر / والد middle, سازمان alone bottom). Form looks centered. **BUT:** navbar at top of /login appears entirely missing (no brand, no theme, no user-menu) — iOS URL bar sits directly above "خوش آمدید" hero with no visible chrome between. | The assertion didn't check that the navbar/Nav is visible on /login. AUTH_FLOW mode hides hamburger + nav-links via CSS, but the brand + actions row should still render. Test never asserted that. | New assertion needed: `expect(page.locator("nav.nav .brand")).toBeVisible()` at /login. Then debug whether the Nav element is in DOM but visually empty, or hidden by some rule. |
| 3 | **B3** — Dashboard + Profile not responsive on mobile | `phase-a-r1-3-fixes.spec.ts:186` dashboard at 375 no horizontal overflow + `:197` stat-row collapses to single col + `:208` profile no h-overflow + stacks 1fr-320px grid | `scrollWidth - innerWidth ≤ 2` on /dashboard and /profile at 375. Stat-row computed `gridTemplateColumns` had 1 value. Profile grid had 1 value. | Not in this screenshot set. Need /dashboard + /profile mobile screenshots after the B4 sidebar fix. | Cannot evaluate without dashboard screenshot. The B4 drawer breakage likely blocked any meaningful dashboard interaction on mobile, so this couldn't be evaluated either. | Pending dashboard screenshot. UNVERIFIED. |
| 4 | **B4** — Workspace mobile sidebar drawer not opening | `phase-a-r1-3-fixes.spec.ts:70` "B4: at 375px, hamburger opens the workspace drawer with sidebar items (not public nav-links)" | After login + goto /dashboard + click `button.nav-toggle`, a `[aria-label='منوی workspace']` element became visible. The Sheet content contained `.side-nav`. | **Screenshot 1:** Drawer DOES open (overlay visible, page dimmed). But content is wrong — **3 horizontal pills** ("ثبت‌نام", "تقویم", "دروس من") on a single row, not the vertical scrollable list of ~25 items with section headers ("یادگیری", "منابع", "هوش مصنوعی", ...). The drawer is essentially unusable. | The assertion checked the Sheet element was visible and that it CONTAINED `.side-nav`. It did not check the layout INSIDE `.side-nav`. styles.css line 3598-3641 transforms `.side-nav` at `<720px` into a horizontal flex container with `!important` (this was for the legacy inline sidebar on `.dash` pages). The Sheet drawer's RoleSideNav inherits the rule. | **`apps/web/styles.css:3598-3641`** — scope the horizontal-pill transform to ONLY `.dash .side-nav` (the inline legacy use), NOT to `.side-nav` globally. The Sheet-wrapped sidebar needs vertical layout. Add new rule `.appshell-sidebar-drawer .side-nav { display: block; ... }` to undo any inherited mobile rules. Plus assertion (D12): `boundingClientRect().height > 300` (sidebar is tall, not pill-thin) + per-item `boundingClientRect().y` increases monotonically (vertical stack). |
| 5 | **B5** — Landing leaks logged-in user info | `phase-a-r1-3-fixes.spec.ts:262` "authed student on / never sees Nav before redirect" | requestAnimationFrame poll in the browser saw `nav.nav` never co-exist with pathname `/` or `/home`. Redirect to `/dashboard` confirmed. | **Screenshot 4:** the avatar "نر" (cyan circle with Persian initials "نر") is clearly visible in the top navbar on `/` landing — even though I cannot tell from the screenshot whether the user is actually authed or anonymous. **Screenshot 5:** same avatar "نر" visible on /login if the navbar is there. | The R1.3 B5 fix gated AppShell from rendering Nav when authed-on-landing. But the real leak isn't Nav rendering; it's that whenever Nav DOES render (PUBLIC route + anon visitor, or AUTH_FLOW route), it shows `role.avatar` = "نر" from the MOCK ROLES dict in `apps/web/src/role.tsx:104` (`student: { avatar: "نر" }`). Phase 14.8 fixed `user-name` but NOT `user-avatar`. The avatar leaks the mock identity to every anonymous visitor. | **`apps/web/src/shared.tsx`** — change the avatar render to use a generic icon (or `role.label[0]`) when `!auth.user`, mirroring the Phase 14.8 user-name fix exactly. Visual baseline: anonymous visitor on `/` shows a neutral mark, not a person-coded initial. Assertion (D12): `expect(page.locator("nav .avatar")).not.toContainText("نر")` at /about as anonymous. |
| 6 | **D9** — Sidebar hamburger-toggle on every viewport | `phase-a-r1-3-fixes.spec.ts:88` lg hamburger visible + `:97` localStorage pref toggles + `:115` ≥3xl pinned-inline + `:132` <3xl always drawer | Hamburger visible at 1280. localStorage `digiu_sidebar_pref` flipped on click. At 1536 with pref=open, `.workspace-grid[data-sidebar-pinned]` rendered. At <1536, even with pref=open the Sheet drawer opened. | Screenshot 1 shows the drawer DOES open. The hamburger-everywhere behavior works structurally. The CONTENT inside the drawer is broken (see B4). | The D9 mechanism works — the rendering bug is downstream in `.side-nav` styling. D9 itself is OK. | None for D9 directly. B4 fix subsumes the visible failure. |
| 7 | **Brand** — JDO logos, footer copyright, About paragraph | `phase-a-r1-3-fixes.spec.ts:219`–`:259` four assertions on `.org-attribution-full`, `.org-attribution-compact`, About-page paragraph fragments | All four selectors found in DOM. Persian text fragments visible. `<img>` elements with correct `src` attributes attached. | **Screenshot 3 (8:37):** the footer org section text is correctly rendered, but the logo shows the broken-image "?" placeholder. The Persian copyright text is good. This screenshot was taken BEFORE the logo file deploy (my logo commit `6018ed6` landed at ~8:36 but the deploy with the rebuilt nginx container happens on next `up`). | Not actually a code bug — a deploy-timing artifact. The logo files exist on disk (I confirmed `apps/web/public/logos/jdo-{light,dark}.png` after the copy) but the running nginx container is the pre-logo image. A fresh `up` rebuilds the nginx image with logos baked in. Visual baseline needed AFTER deploy. | `.\scripts\remote.ps1 up` once more so the logo files land in the nginx container's `/usr/share/nginx/html/logos/`. Then assertion (D12): `await expect(page.locator("img.org-attribution-logo.is-dark")).toHaveScreenshot("jdo-dark-72px.png", { threshold: 0.001 })`. |

## Procedure once screenshots arrive

1. For each row, fill columns 4–6 from the screenshot.
2. The fix column lists the **exact** file + line to change. No hand-wavy "improve responsive" — only "Auth.tsx:215 add `.role-tab-strip-fixed` class with explicit 2-col CSS, no inline."
3. For each row, write a NEW assertion (per D12 — the 5-point visual contract) that would have caught what the screenshot shows.
4. Commit the filled audit. Then execute the fixes one bug per commit. Then re-verify with the new assertions AND a follow-up manual smoke.

## What the rule says now

- **D12** — every visual claim asserts 5 things, not just `toBeVisible()`. See `PHASE_A_DECISIONS.md#R1.3-D12`.
- **D13** — manual smoke is a formal gate; automated pass without manual pass is not a pass. See `PHASE_A_DECISIONS.md#R1.3-D13`.

Both rules saved to memory so they outlive this session.

## What does not happen until screenshots arrive

No R1.4 code. No R2. No new specs. No retries. Waiting.
